#ifndef LZSS_H
#define LZSS_H

// TODO: Provide connections to lzss.c
// NOTE: uses global infile & outfile, may need to create new functions:
// -- int encode_buffer(char *src, int src_len; char *dest) {...};  // returns dest_len
// -- int decode_buffer(char *src, int src_len; char *dest) {...};  // returns dest_len


#endif
